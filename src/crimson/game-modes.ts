// Port of crimson/game_modes.py

/** Known `game_mode` ids from the original config / highscore tables. */
export enum GameMode {
  DEMO = 0,
  SURVIVAL = 1,
  RUSH = 2,
  QUESTS = 3,
  TYPO = 4,
  TUTORIAL = 8,
}
